import { SetSpanColorDirective } from './set-span-color.directive';

describe('SetSpanColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SetSpanColorDirective();
    expect(directive).toBeTruthy();
  });
});
